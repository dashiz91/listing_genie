{
  "name": "listing-genie",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -k -n \"API,WEB\" -c \"yellow,cyan\" \"npm run backend\" \"npm run frontend\"",
    "backend": ".\\venv\\Scripts\\python -m uvicorn app.main:app --reload",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
